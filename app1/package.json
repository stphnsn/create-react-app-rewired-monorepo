{
  "name": "app1",
  "version": "0.1.0",
  "scripts": {
    "start": "PORT=3000 react-app-rewired start --config-overrides ../config-overrides.js",
    "build": "react-app-rewired build --config-overrides ../config-overrides.js",
    "test": "react-app-rewired test --env=jsdom --config-overrides ../config-overrides.js",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@monorepo/jest-config": "^1.0.0",
    "@monorepo/eslint-config": "^1.0.0"
  }
}
